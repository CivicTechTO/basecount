import React, { Component } from "react";
import { Section } from "../utilities";

export default class Login extends Component {
  render() {
    return (
      <Section>
        This is the home page. Stuff should be here, eventually.
      </Section>
    );
  }
}